# include <bits/stdc++.h>
# define rep(i,n) for(int i=0; i<(n); ++i)
using namespace std;
using vi = vector<int>;

int main() {
   int n;
   cin >> n;
   vi c(n-1), s(n-1), f(n-1);
   rep (i, n-1) cin >> c[i] >> s[i] >> f[i];

   return 0;
}
