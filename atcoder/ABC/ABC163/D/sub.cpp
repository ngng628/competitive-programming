# include <bits/stdc++.h>
using namespace std;

# define rep(i, a, cmp, b) for (int i = a; i cmp b; ++i)

int main() {
   rep (i, 0, <, 10) cout << i << ' ';
   cout << '\n';
   rep (i, 0, <=, 10) cout << i << ' ';
   cout << '\n';
}