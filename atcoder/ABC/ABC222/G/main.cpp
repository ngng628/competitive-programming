# include <bits/stdc++.h>
using ll = long long;
using namespace std;

struct Setup_io {
   Setup_io() {
      ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0), cerr.tie(0);
   }
} setup_io;

int main() {
   vector<int> cache(100'000'000);
   int val = 2;
   rep (_, 100'000'000) {
      cache[]
   }

   int T;
   cin >> T;
   while (T--) {
      
   }
}