# include <bits/stdc++.h>
# define ALL(v) (v).begin(), (v).end()
# define rep(i,n) for(int i=0;i<(n);i++)
using namespace std;

void Main() {

}

int main() {
    while (true) {
        int n, m, t, p;
        cin >> n >> m >> t >> p;
        if (n == 0 and m == 0 and t == 0 and p == 0) break;

    }

    return 0;
}

